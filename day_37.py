#-------- DAY 37 "arrays1" --------
# 1. Create an array
# 2. Assign to the array
# 3. Access the array items by referring to the index number
# 4. Change an item value
# 5. Use len() method to count the number of length, as we used with strings, lists, tuples, sets & dictionaries
arr = ['list', 'tuple', 'set', 'dictionary', 'array']
text = "Lesson {}: {} 1"
arr[4] = 'Arrays'
print(text.format(len(arr)*7+2, arr[4]))
